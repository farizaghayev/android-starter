default_platform(:android)

platform :android do
  desc "Run the unit tests"
  lane :beta do
    gradle(task: "test")
  end

  desc "Assemble a debug apk"
  lane :prod do
    gradle(task: "assembleDebug")
  end

end